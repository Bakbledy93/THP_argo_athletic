          <h4>Editer le profile de l'user <%=@profile.user.id%></h4>
          <%= form_for @profile, url: admins_profile_path do |f| %>
          <div class="col-lg-6 bg-white py-6 d">
            <div class="col h-100 align-items-center justify-content-center">
              <div class="col-sm-8 col-md-6 col-lg-10 col-xl-8"
                   data-zanim-xs='{"animation":"slide-right"}'
                   data-zanim-trigger="scroll"
              >
           
                <form class="mt-5">
                  <div class="row mb-4">
                    <div class="col">
                      <%= f.text_field :first_name,
                        class:"form-control border-300 bg-light",
                        autofocus: true,
                        placeholder: 'prénom' 
                      %> 
                    </div>
                  </div>

                  <div class="row mb-4">
                    <div class="col">
                      <%= f.text_field :last_name,
                          class:"form-control border-300 bg-light",
                          autofocus: true,
                          placeholder: 'nom' 
                      %> 
                    </div>
                  </div>
                  <div class="row mb-4">
                    <div class="col">date de naissance
                        <%= f.date_field :date_of_birth, as: :date, 
                          class:"form-control border-300 bg-light",
                          autofocus: true,
                          placeholder: 'date de naissance' 
                      %> 
                    </div>
                  </div>
                  <div class="row mb-4 flex">
                    <div class="col">Votre poids
                          <%= f.text_field :weight, 
                          class:"form-control border-300 bg-light",
                          placeholder: "Votre poids" 
                      %> 
                    </div>
                  </div>


                  <div class="input-group mb-3">
                    <div class="input-group-prepend">
                      <label class="input-group-text" for="inputGroupSelect01">Choix du sport</label>
                    </div>
                    <%= f.select :sport, @sports_array, 
                        class:"form-control border-300 bg-light btn",
                        placeholder: "Choix du poste"
                      %> 
                  </div>


                  <div class="input-group mb-3">
                    <div class="input-group-prepend">
                      <label class="input-group-text" for="inputGroupSelect01">Choix du poste</label>
                    </div>
                    <%= f.select :sport_role, @roles_array, 
                        class:"form-control border-300 bg-light btn",
                        placeholder: "Choix du poste"
                      %> 
                  </div>

                  <div class="input-group mb-3">
                    <div class="input-group-prepend">
                      <label class="input-group-text" for="inputGroupSelect01">Niveau</label>
                    </div>
                    <%= f.select :level, @levels_array, 
                        class:"form-control border-300 bg-light btn",
                        placeholder: "Choix du poste"
                      %> 
                  </div>

                  </div> 

                  <div class="row mb-4 align-items-center">
                    <div class="col-auto">
                      <%= f.submit :"Confirmer vos données",
                          class:"btn btn-dark btn-block mt-3",
                          autofocus: true 
                      %>   

                      <%= link_to "Retour", admins_users_path, class:"btn btn-outline-dark btn-block mt-3" %>            
                    </div>
                    <%end%>